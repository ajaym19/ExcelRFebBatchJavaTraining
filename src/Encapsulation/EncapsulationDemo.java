package Encapsulation;

public class EncapsulationDemo {

	/*
	 * Encapsulation	
	 * trying to hide the data
	 * 
	 * wrap our variables with methods
	 * also known as Getters and Setters
	 * 
	 * 
	 * can be achieved by Access Modifiers
	 * private: within the class
	 * default: within the package
	 * protected: within the package + outside the package only by child class
	 * public: within the entire project
	 * 
	 * 
	 */
}
