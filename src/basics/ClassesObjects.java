package basics;

public class ClassesObjects {

	/*
	 * Objects:
	 * a real time entity
	 * pen, pencil, car, mobile
	 * 
	 * Object has
	 * 1. Properties: which defines the object
	 * 				are defined as variables
	 * 2. Behavior: what actions can be performed by the object
	 * 				are defined as functions/methods
	 * pen
	 * properties: type, brandname, color, length, weight, 
	 * behavior: write, 
	 * 
	 * 
	 * human being:
	 * properties: name, id, height, weight, age, pancard, aadharcard, nationality, driveing number..
	 * behavior: walking, reading, writing, thinking, sleeping and eating
	 * 
	 * classes:
	 * blueprint which contain objects
	 * 
	 * identify the properties for some objects
	 * like books, computer etc..
	 * 
	 * 
	 * 
	 */
}
